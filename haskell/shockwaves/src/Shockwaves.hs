


module Shockwaves (
    Display(..),
    ValueRepr(..),ValueKind(..),
    Split(..),
    VariableInfo(..),
    translate,
) where

import Shockwaves.Viewer (Display(..),Split(..),ValueRepr(..),ValueKind(..),VariableInfo(..),translate)
